<?xml version="1.0" encoding="utf-8"?>
<!-- 桌面待机屏中间内容（碎片）：240dp 宽，高度 match_parent 自适应中间容器。
     顶栏与快捷栏（含上下点线）位置固定优先保障；通知/组件区填充剩余空间，
     允许被挤压（后续会做可滚动）。基类缩放不再触发二次缩小分支。
     运营商名(中国移动/联通等)已迁到 nokia_top_bar.xml，由 StatusBarController 统一管理。 -->
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="240dp"
    android:layout_height="match_parent">

    <!-- 桌面组件区：从顶部开始填充（快捷栏已移到 Activity 级底栏上方），
         动态渲染用户添加的组件，允许被挤压（后续会做可滚动）。 -->
    <LinearLayout
        android:id="@+id/notificationArea"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_alignParentTop="true"
        android:orientation="vertical"
        android:paddingStart="8dp"
        android:paddingTop="4dp"
        android:paddingEnd="8dp"
        android:paddingBottom="4dp" />


    <!-- 桌面环形装饰：大小不一分布在各处，透明度低，不遮挡交互。
         均避开底部快捷栏区域（底部装饰留出足够 marginBottom），保证完整显示。 -->
    <ImageView
        android:id="@+id/decoRingLargeTopRight"
        android:layout_width="90dp"
        android:layout_height="90dp"
        android:layout_alignParentTop="true"
        android:layout_alignParentEnd="true"
        android:layout_marginTop="10dp"
        android:layout_marginEnd="14dp"
        android:alpha="0.12"
        app:srcCompat="@drawable/deco_rings" />

    <ImageView
        android:id="@+id/decoRingMidTopLeft"
        android:layout_width="60dp"
        android:layout_height="60dp"
        android:layout_alignParentTop="true"
        android:layout_alignParentStart="true"
        android:layout_marginTop="44dp"
        android:layout_marginStart="12dp"
        android:alpha="0.10"
        app:srcCompat="@drawable/deco_rings" />

    <ImageView
        android:id="@+id/decoRingMidRight"
        android:layout_width="70dp"
        android:layout_height="70dp"
        android:layout_alignParentEnd="true"
        android:layout_centerVertical="true"
        android:layout_marginEnd="6dp"
        android:alpha="0.10"
        app:srcCompat="@drawable/deco_rings" />

    <ImageView
        android:id="@+id/decoRingSmallLeft"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_alignParentStart="true"
        android:layout_centerVertical="true"
        android:layout_marginStart="6dp"
        android:alpha="0.10"
        app:srcCompat="@drawable/deco_rings" />

    <ImageView
        android:id="@+id/decoRingBottom"
        android:layout_width="70dp"
        android:layout_height="70dp"
        android:layout_alignParentBottom="true"
        android:layout_alignParentEnd="true"
        android:layout_marginBottom="48dp"
        android:layout_marginEnd="10dp"
        android:alpha="0.12"
        app:srcCompat="@drawable/deco_rings" />
</RelativeLayout>
